{
  "name": "ts-parser-combinator-practice",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf coverage build tmp",
    "init": "mkdir -p build",
    "build": "npm run init && tsc",
    "start": "node dist/math-parser.js",
    "watch": "nodemon -e ts --exec 'npm build && npm start'",
    "postinstall": "npm run build"
  },
  "author": "Seyedamirhossein Hesamian",
  "license": "ISC",
  "dependencies": {
    "parsimmon": "^1.13.0",
    "util": "^0.12.1"
  },
  "devDependencies": {
    "@types/parsimmon": "^1.10.0",
    "nodemon": "^2.0.2"
  }
}
